#include <iostream>

struct node{
  int val;
  node *next;
}:

int main(){
  do{
    int choice = 0 ;
    cin >> choice;
    switch (choice){
      case 1:
      std::cout << "inserting ->" << '\n';
      cin >> x;
      insert(x)
    }

  }while(choice != 0);
  return 0;
}
